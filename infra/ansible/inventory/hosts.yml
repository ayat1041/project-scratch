---
all:
  children:
    webservers:
      hosts:
        app:
          ansible_host: "CHANGE_ME" # server IP, set per real deployment
          ansible_port: 22
          ansible_user: "{{ admin_user }}"
