---
- name: Converge
  hosts: all
  gather_facts: true
  become: true
  pre_tasks:
    - name: Create test user
      ansible.builtin.user:
        name: testuser
        state: present
  roles:
    - role: docker
