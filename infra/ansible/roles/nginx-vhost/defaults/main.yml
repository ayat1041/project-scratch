---
# ssl (default): deploy temp HTTP vhost → obtain Let's Encrypt cert → deploy full HTTPS vhost
# temp:          deploy HTTP-only vhost (useful for local testing or pre-DNS cutover)
vhost_mode: ssl

nginx_sites_available: /etc/nginx/sites-available
nginx_sites_enabled: /etc/nginx/sites-enabled
nginx_service_name: nginx
