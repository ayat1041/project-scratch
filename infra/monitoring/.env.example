# Copy to .env and fill in real values before starting the stack
# cp .env.example .env

# Grafana admin credentials — change before first boot
GF_ADMIN_USER=admin
GF_ADMIN_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# Public domain for Grafana (used in GF_SERVER_ROOT_URL)
GF_DOMAIN=monitoring.example.com
